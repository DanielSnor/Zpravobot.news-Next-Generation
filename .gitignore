# Private documentation (contains real IPs, tokens, credentials)
docs-private/
docs - OLD/
*.private.md

# Secrets and credentials
# Vzory končí * záměrně — pokrývají i odkládací kopie (-copy, .bak, .orig).
# Šablony .example se vracejí zpět negací na konci bloku.
env.sh
config/mastodon_accounts.yml*
config/mastodon_accounts_*
config/bluesky_accounts.yml*
config/global.yml*
config/sources/*
!config/*.example

# Logs
*.log
log/
logs/

# Temp
tmp/

# Runtime data
queue/
cache/
output/
*.lock

# Ruby
*.gem
.bundle/
vendor/bundle/

# macOS
.DS_Store

# Nova IDE
.nova/

# Claude Code (per-user local config a paměť)
CLAUDE.md
.claude/
