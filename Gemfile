# frozen_string_literal: true

source 'https://rubygems.org'

# PostgreSQL adapter
gem 'pg'

# IDN/punycode support (internationalized domain names)
gem 'simpleidn'

# RSS/Atom feed parsing (moved from default gems in Ruby 3.4)
gem 'rss'

# Logger (moved from default gems in Ruby 3.5; add now to silence warning)
gem 'logger'
